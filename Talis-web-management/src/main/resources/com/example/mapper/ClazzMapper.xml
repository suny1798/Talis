<!--定义Mapper映射文件的约束和基本结构-->
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.ClazzMapper">



    <select id="list" resultType="com.example.pojo.Clazz">
        select
            clazz.*,
            emp.name as masterName,
            case
                when clazz.end_date &lt; now() then '已结课'
                when clazz.begin_date &gt; now() then '未开班'
                else '在读中'
            end as status
        from clazz left join emp on clazz.master_id = emp.id
        <where>
            <if test="name != null and name != ''">
                and clazz.name like concat('%', #{name}, '%')
            </if>
            <if test="begin != null and end != null">
                and clazz.end_date between #{begin} and #{end}
            </if>
        </where>
        order by clazz.update_time desc

    </select>
</mapper>